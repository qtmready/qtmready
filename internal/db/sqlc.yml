version: "2"
sql:
  - engine: "postgresql"
    schema:
      - "./migrations/000001_setup.up.sql"
    queries:
      - "./queries/user.sql"
      - "./queries/team.sql"
    gen:
      go:
        package: "entities"
        out: "./entities"
        sql_package: "pgx/v5"
